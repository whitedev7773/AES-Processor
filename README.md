# AES 프로세서

이 프로젝트는 AES(Advanced Encryption Standard) 알고리즘을 C, C++, Verilog로 구현하는 것을 목표로 합니다.

## 🚀 소개

AES 암호화 및 복호화 프로세서의 다양한 구현을 포함하는 프로젝트입니다. 각 구현은 서로 다른 프로그래밍 언어와 패러다임을 사용하여 알고리즘의 소프트웨어 및 하드웨어 측면을 탐색합니다.

## ✨ 주요 기능

*   **AES-128 암호화 및 복호화:** 표준 AES-128 알고리즘을 지원합니다.
*   **다중 구현:**
    *   C
    *   C++
    *   Verilog (예정)
*   **명령줄 인터페이스:** 간편한 테스트 및 자동화를 위한 CLI를 제공합니다.

## 🛠️ 설치 및 실행

### C 구현

`c/` 디렉터리에 있는 C 구현을 빌드하고 실행하는 방법입니다.

**빌드:**

```bash
make -C c/
```

**실행:**

```bash
# 파일로부터 암호화
./c/aes --encrypt --key c/key.txt --input c/input.txt --output c/output.txt

# 명령줄 텍스트로부터 암호화
./c/aes --encrypt --key c/key.txt --text "HEX_STRING" --output c/output.txt
```

### C++ 구현

`cpp/` 디렉터리에 있는 C++ 구현을 빌드하고 실행하는 방법입니다.

**빌드:**

```bash
make -C cpp/
```

**실행:**

```bash
# 파일로부터 암호화
./cpp/aes --encrypt --key c/key.txt --input c/input.txt --output cpp/output.txt

# 명령줄 텍스트로부터 암호화
./cpp/aes --encrypt --key c/key.txt --text "HEX_STRING" --output cpp/output.txt
```

**테스트:**

단위 테스트를 실행하려면 다음 명령을 사용하세요.
```bash
make test -C cpp/
```

### Verilog 구현 (예정)

(추후 업데이트 예정)

## ✍️ 사용법

자세한 사용법 및 각 구현별 옵션은 각 디렉터리의 `README.md` 파일을 참고하십시오. (현재 C 구현에 대한 설명은 최상위 `README.md`에 포함되어 있습니다.)

## 📄 라이선스

이 프로젝트는 [MIT 라이선스](LICENSE)를 따릅니다.
